{
  "name": "@payload/balance-lab",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint src test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "report": "pnpm build:deps && node dist/cli.js",
    "dominance": "pnpm build:deps && node dist/dominance-cli.js",
    "build:deps": "pnpm -w exec turbo run build --filter=@payload/balance-lab..."
  },
  "dependencies": {
    "@payload/sim": "workspace:*",
    "@payload/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
