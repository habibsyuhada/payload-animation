import { Screen } from "./Screen.js";

export function VirusLab(): JSX.Element {
  return (
    <Screen title="Virus Lab">
      <p>Builder rantai blok (drag &amp; drop vertikal), simulasi kering vs pertahanan latihan. (GDD §12.2 / PLAN.md C3.2 — belum diimplementasikan.)</p>
    </Screen>
  );
}
