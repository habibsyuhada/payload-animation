import { Screen } from "./Screen.js";

export function Scan(): JSX.Element {
  return (
    <Screen title="Scan / Attack">
      <p>Daftar target → konfirmasi → replay. (GDD §12.4 / PLAN.md Fase 4 — belum diimplementasikan, butuh server.)</p>
    </Screen>
  );
}
