import { Screen } from "./Screen.js";

export function DefenseGrid(): JSX.Element {
  return (
    <Screen title="Defense Grid">
      <p>Editor topologi (pinch-zoom, drag node), validator jalur otomatis. (GDD §12.3 / PLAN.md C3.3 — belum diimplementasikan.)</p>
    </Screen>
  );
}
