import { Screen } from "./Screen.js";

export function Replay(): JSX.Element {
  return (
    <Screen title="Replay Player">
      <p>Layar penuh, scrub bar + markers + speed control + export. (GDD §12.5 — packages/replay dan packages/ui/Scrubber sudah ada, integrasi ke screen ini belum dikerjakan.)</p>
    </Screen>
  );
}
