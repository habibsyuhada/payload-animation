import type { DefenseNodeType } from "../types.js";

/**
 * Patch Server — v2-only node type (RULESET.md §14, ADR 0007 §A). Placeholder for 8.1b's 1:1 file
 * layout; real tables and behavior land in PLAN.md 8.2a alongside this file's tests.
 */
export const NODE_TYPE: DefenseNodeType = "patch-server";
